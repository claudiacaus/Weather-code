const weatherContainer = document.createElement('div');
weatherContainer.setAttribute('class', 'weather-container');
document.body.appendChild(weatherContainer);

const weatherInformation = document.createElement('div');
weatherInformation.setAttribute('class', 'weather-information');
weatherContainer.appendChild(weatherInformation);

const searchCity = document.createElement;
var questionText = document.createTextNode('Is the sky blue?');
weatherInformation.appendChild(questionText);
